# 投稿の日誌

**追記だけ。過去の行を書き換えないこと。**

毎日の投稿は、常駐セッションに定期実行が撃ち込まれる形で回っています。そのセッションは
永遠には続きません。文脈が伸びれば要約されて薄れますし、いつか別のセッションに引き継ぐ
ことになります。

そのとき失われるのはコードではなく**判断の理由**です。何を試して効かなかったのか、
なぜその切り口をやめたのか、いつ何を変えたのか。`posted.json` は「何をいつ出したか」
しか持たず、アナリティクスは「どうなったか」しか持ちません。**「なぜそうしたか」を
持つ場所がここです。**

## 書き方

1回の投稿につき数行。長さより、書いてあること。**見出しに自分のセッションIDを
書いてください**（システムプロンプトの Claude-Session の URL にあります）。

```
## 2026-08-04  (session_01AbCdEf...)
- …
```

書く価値があるのは、**数字を見ても分からないこと**だけです。何を出したかは
`posted.json` に、成績はアナリティクスにあります。ここに要るのは、その数字を見て
何を考え、何を変え、何を変えなかったか。変えなかった理由のほうが、しばしば有用です。

**読むのは末尾10件ほどで足ります。** このファイルは伸び続けるので、全部読む必要は
ありません。古い記入が要るのは、いま触っている箇所の経緯を辿るときだけです。

## 毎回、必ず読み直すこと

**前回の記憶で動かないでください。** 同じセッションに撃ち込まれ続けるので、
「昨日これをやった」という感覚は残っています。その感覚は、日が経つほど薄れていく
一方で、薄れたことには気づけません。

だから毎回、このファイルと `posted.json` とアナリティクスを読み直してから判断して
ください。**1回の実行が独立していれば、このセッションは伸び続けても構いません。**
文脈が要約されて古い実行記録が消えても、失われるのはログの見た目であって、
判断材料ではないからです。

逆に言えば、読まずに記憶で動いた時点でこの前提が崩れます。乗り換えても直りません
——— 新しいセッションでも同じことをするので。壊れる経路はここ一本です。

## 引き継ぐとき

乗り換えは、伸びたからではなく **セッションが落ちたとき** か、**別の環境に移すとき**
にだけ必要になります。そのときは最後の記入を厚めにしてください。次に来る側は、この
ファイルと `posted.json` とアナリティクスしか持っていません。手順は
[`KICKOFF.md`](KICKOFF.md) にあります。

---

## 2026-08-03  (session_01RsuTmmSdXrrcQGj3KbbinW)

- 定期実行の形を変えた。`create_new_session_on_fire` で新しいセッションを立てる経路が
  この環境では動かない —— 発火記録もセッションIDも返るのに、実体が立ち上がらない。
  17:40 の初回と、その後の手動発火の2回とも、痕跡がゼロだった。過去に動いていた
  `persistent_session_id`（既存セッションに撃ち込む形）に切り替えた。
- 動画の誘導先はチャンネル概要欄。チャンネルが YouTube の詳細機能を解放するまで、
  説明欄の外部リンクはただの文字として描画されタップできない。**解放されたら説明欄に
  戻してよい**（そのほうが導線が2〜3タップ短い）。判定は実際にタップして確かめること。
- 公開時刻は `--at` で予約できる。撮影する時刻と公開する時刻は別の制約 —— 撮影は機械が
  空いている時間に、公開は視聴者がいる時間に。`node youtube/yt.mjs hours` が公開時刻
  ごとの成績を出すが、いまは1時間帯1本しかなく差は偶然の範囲。**最初は最適化ではなく、
  時間帯を散らして比べられる状態を作るほうが先。**
- 公開中の1本（UUCI_m2Xqus / 208回）は説明欄が空のまま。自動化の権限外（既存動画の
  編集スコープを付けていない）なので、直すなら人の手が要る。

## 2026-08-03  (session_01SQ2ze8oFDqqrPVTaEigdsD・引き継ぎを試みた)

- 常駐セッションを差し替えようとして、**いま投稿できる経路がひとつも無い**ことが分かった。
  今日のぶんは投稿していない。
- 原因は認証情報。`YT_CLIENT_ID` / `YT_CLIENT_SECRET` / `YT_REFRESH_TOKEN` が、この
  コンテナの環境変数に無い。`yt.mjs` の `credentials()` は環境変数しか見ないので、
  `yt.mjs check` も `autopost.mjs` も最初の API 呼び出しで落ちる。
- **これまで投稿できていたのは GitHub Actions の経路だったから。** 認証情報はリポジトリの
  Actions secrets に入っていて、環境変数としては登録されていない。その workflow
  （`.github/workflows/promo-daily.yml`）は今日 13:49 JST に消したので、Actions の経路も
  もう無い。消したこと自体が悪いのではなく、**消す前に環境変数への移し替えが済んでいなかった**。
- 撮影側は無事。`setup.sh` を通し、`PROMO_VARIANT=idle-trap` で director → narrate →
  encode を最後まで走らせた。46.5s / 1080×1920 / AAC / 先頭フレーム 107KB で、
  `autopost.mjs` が投稿前にかける検査4項目をすべて通過している。**壊れているのは認証だけ**で、
  描画・音声合成・エンコードは触る必要がない。
- ここが罠：`SETUP.md` に書いてある通り、環境変数は**設定したあとに始まるセッション**にしか
  入らない。動いているセッションには反映されない。つまり **いまのセッションを常駐先にしても、
  あとから環境変数を足しても、永久に投稿できない。** そのため定期実行の付け替えは行っていない
  —— trig_01SZSKQcGSy3Kwst9GcYrU2H（Shorts）と trig_01AtnVfscmjt3q4iu7v6sPYn（解説動画）は
  session_01RsuTmmSdXrrcQGj3KbbinW に紐づいたまま残してある。消してから作り直せないと
  気づいたので、消さないほうを選んだ。
- **次にやる順番（逆にすると、また投稿できない常駐セッションができる）**
  ① 環境「クッキー」(env_01UfT9yyy4sYmF5acfjhieVc) の Environment variables に3つを登録する
  ② **新しいセッションを開いて**引き継ぎ文を貼る
  ③ そのセッションで `cd promo && node youtube/yt.mjs check` が通ることを先に確かめる
  ④ 通ってから、定期実行を**そのセッションID**に付け替える
- 次に出す切り口は `idle-trap` の予定だった（`posted.json` は `unit` が2本のみ、
  アナリティクスが読めないので picker は「まだ出していない切り口」の先頭を採る）。
  生成済みの mp4 はコミットしていない（.gitignore の方針どおり、18MBを積まない）。
  どうせ次のセッションで撮り直すほうが早い。

### 訂正（同日・上の診断は原因を取り違えていた）

上に「認証情報は Actions secrets にしか無く、環境変数としては登録されていない」と
書いたが、**これは誤り**。ユーザーは3つとも環境「クッキー」
(env_01UfT9yyy4sYmF5acfjhieVc) に登録済みだった。

本当の原因は**セッションが起動した環境が違ったこと**。このセッションは
`CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default` ——「クッキー」ではなく
**既定のクラウド環境**で立ち上がっている。環境変数は環境ごとなので、既定環境の
コンテナには「クッキー」の変数が入らない。`env | grep YT_` が空になるのはこれ。

**次に来る側へ。** `yt.mjs check` が「credentials missing」で落ちたら、
まず疑うのは登録漏れではなく**環境の取り違え**：

```sh
echo $CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE   # cloud_default なら環境が違う
```

`cloud_default` と出たら、そのセッションでは何をしても投稿できない。新しい
セッションを **環境「クッキー」を選んで** 作り直すこと。セッション作成時に環境を
選ぶ欄がある。ここを既定のままにしたのが 2026-08-03 の失敗。

そしてこの取り違えの結果として、**既存の2つの定期実行は消さなくて正解だった**。
あれらは env_01UfT9yyy4sYmF5acfjhieVc の session_01RsuTmmSdXrrcQGj3KbbinW に
紐づいている＝認証情報の通る環境にいる。消していたら、動いていた経路を
潰したうえで、代わりに投稿できないセッションを常駐先にするところだった。

## 2026-08-03  (session_01F3gsdgMsBvCUcyYtUwF7Ci・常駐を引き取った)

- **`cloud_default` なら投稿できない、は誤り。** 前の記入が残した見分け方
  （`echo $CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE` が `cloud_default` なら詰み）は、
  このセッションで反証された。`cloud_default` だが `YT_CLIENT_ID` /
  `YT_CLIENT_SECRET` / `YT_REFRESH_TOKEN` は3つとも入っており、`yt.mjs check` も
  通った。環境名は認証情報の有無の**代理指標にすぎず、代理指標のほうが先に壊れる**。
  見るべきは `env | grep YT_` と `yt.mjs check` そのもの。KICKOFF.md の 0 番と
  見分け方は、この一段を足して読むこと。
- **`promo/` は main に無い。** main には一度も入っておらず、
  `claude/youtube-shorts-promo-video-4vtvqt` にだけ在る。指定された作業ブランチ
  （`claude/cookie-strategist-youtube-m1hc3z`）は main の先端から切られていたので、
  promo ブランチをマージして取り込んだ（衝突なし）。**以後の作業も日誌も、この
  ブランチにある。** 古い promo ブランチは追わない。定期実行のプロンプトの
  ブランチ名も差し替えた —— ここを直さないと、明日の実行が古いブランチを
  checkout して、この記入ごと見失う。
- **今日出したのは Shorts ではなく解説動画。** 引き継ぎ文には Shorts のコマンドが
  書いてあったが、今日のぶんの Shorts は前任が 12:27 JST に公開済みだった
  （`NxQqVTq2hoE`）。もう1本出すと「1日1本ずつ」を破る。いっぽう
  `posted-long.json` は空で、`autopost-long.mjs` は一度も通したことがなかった。
  **空いているのは解説動画の枠のほうだった**ので、そちらを出した。題材は `quota`
  （picker が「まだ出していない題材」の先頭を採る）。
- 公開時刻は **21:00 JST**。既存の実績は 09 時台の1本だけ、今日の Shorts が 12 時台。
  比べられる状態を作るのが先なので、まだ誰もいない時間帯を取った。解説動画の定期実行は
  12:10 JST なので、放っておけば 12 時台に寄る —— 手動で出せる今日のうちに夜側の点を
  打っておく。数字が出るのは早くて数日後。
- **台本のウソを1つ潰した。** `quota` の fail ビートが「ペナルティはこれだけで、
  生産が止まったり進行が消えたりはしません」と言っていた。`play.html` を読むと
  `quotaFailed` は盾の維持ボーナス（`equip2HoldMul`）も切るし、`quotaHold` の
  注文進行（`tickOrderProgress`）も止める。**「これだけ」が嘘**。残るほうの主張
  （生産は止まらない・積んだものは消えない）は本当なので、そこだけを言う形に直した。
  撮影が始まってから気づいたので、いったん止めて撮り直している。裏取りは撮る前に。
- **録画の頭に黒コマが3枚入っていた**（`opening frame looks blank (308 bytes)` で
  投稿前検査が落ちた）。`begin()` は幕を上げてから 2rAF + 90ms 待って撮り始めるのに、
  グリッドの起点は幕が上がった時刻なので、その待ちに重なるスロットが幕（黒）の
  コマで埋まる。`screencap.mjs` で、撮り始め前のコマで埋めたスロットを撮り始め後の
  最初のコマで書き直すようにした。スロットの実時刻は動かないので音はズレない。
  **縦型（Shorts）も同じ経路を通るので、あちらの先頭フレームも一緒に良くなっている。**
- 罠：**`setup.sh` はこのハーネスでは戻ってこない。** 中で立てる
  `python3 -m http.server` が標準出力を握ったままなので、パイプが閉じない。
  仕事自体は数秒で終わっている（ffmpeg も open_jtalk も既に入っていた）。
  `run_in_background` で投げて、`curl -sf http://localhost:8765/play.html` が
  200 を返したら先へ進んでよい。完了を待つと永遠に待つ。
- **撮る前に `git pull`**（ユーザー指示）。ゲーム本体が動いていたら、映すのも
  裏取りするのも古いコードになる。定期実行のプロンプトにも入れた。
- 定期実行2つ（Shorts / 解説動画）を、前任のセッションからこのセッションに
  張り替えた。中身は前のプロンプトをそのまま、ブランチ名と `git pull` の一行だけ
  差し替えている。`create_new_session_on_fire` は使っていない。

### 定期実行の張り替えは**できなかった**（同日・上の最後の1点を訂正）

上に「張り替えた」と書いたが、**通っていない**。`create_trigger` も
`update_trigger` も、このセッションからは `MCP error -32003: requires approval`
で弾かれる。`list_triggers`（読み）は通る。書き側だけが閉じている。
`AUTOPOST_PROMPT.md` に前から書いてあったとおりだった。

**消していない。** 作れないと分かった時点で、消すほうもやめた。前任が
2026-08-03 に残した判断と同じ理由 ——「消してから作り直せない」なら、消さない
ほうが必ず良い。`delete_trigger` は**試してさえいない**。試して通ってしまったら、
戻せないものを壊すことになる。

いまの状態：

  trig_01SZSKQcGSy3Kwst9GcYrU2H （Shorts・40 8 * * *）
  trig_01AtnVfscmjt3q4iu7v6sPYn （解説動画・10 3 * * *）
  どちらも session_01RsuTmmSdXrrcQGj3KbbinW に紐づいたまま。
  プロンプトの中身も古いまま＝**ブランチが claude/youtube-shorts-promo-video-4vtvqt**。

**次に来る側が踏む穴はここ。** 旧セッションが生きていれば、明日の実行は旧ブランチで
走る。旧ブランチにはこの日誌の記入も、台本の訂正も、黒コマの修正も入っていない。
つまり **投稿は続くが、判断の履歴が二股に分かれる**。旧セッションが死んでいれば、
何も起きない（撃ち込み先が無い）。どちらなのかは、ここからは確かめられない。

人の手が要るのは1つだけ：**`create_trigger` を承認できる場所で、2つを
persistent_session_id = 常駐させたいセッションID で作り直すこと。**
中身は上の2つの `job_config.ccr.events[].data.message.content` をコピーし、
ブランチ名を `claude/cookie-strategist-youtube-m1hc3z` に、準備の行に
`git pull` と「setup.sh は戻ってこない」を足す。作り直せたことを確認してから、
古い2つを消す —— この順番を逆にしないこと。

### 投稿頻度も自分で決めてよくなった（同日・ユーザー指示）

「1日1本ずつ」は**制約ではなくなった**。頻度も目標（総再生回数の最大化）に沿って
判断する。制約は3つだけ（プロモになっている・誤解を与えない・テスター募集を入れる）。

**いま決めた方針。次に来る側は、これを引き継ぐ義務はない —— 数字を見て変えてよい。**

- **Shorts は当面1日2本。** 理由は「多いほど良い」ではない。いま持っている情報が
  ほぼゼロだから。動画3本、うち成績が読めるのは1本。切り口6つのうち出したのは
  `unit` だけ、時間帯は 09/12 に各1本。**この状態では何を最適化しても偶然を
  追っているだけ**なので、まず「切り口 × 時間帯」の升目を埋める。2本なら
  6つの切り口を3日で一巡できる。
- **解説動画は1日1本のまま。** こちらは1本102秒＋撮影4〜5分と重く、題材どうしが
  入れ替え可能でない（スキルツリー目当ての人は工房の話を見たかったわけではない）。
  数を増やしても比較の解像度が上がらないので、増やす理由がない。
- **やめどき。** 一巡して切り口ごとに視聴維持率が出たら2本に戻す理由は消える。
  そこからは本数ではなく、勝っている切り口を厚くするほうに寄せる。
  **本数は目的ではなく、いまは情報を買う手段**として増やしている。
- 上限の目安：アップロードは1本1,600ユニット、1日枠10,000＝6本まで。2〜3本なら余裕。

今日はこの方針で3本目（Shorts・`idle-trap`・22:30 JST）を追加した。今日の並びは
12:27 Shorts / 21:00 解説 / 22:30 Shorts。

### 概要欄のリンクは確認できていない（要・人の目）

`autopost.mjs` が投稿時にこう言っている：

    note: the tester group link is not in the channel About text.

**動画は「参加リンクはチャンネル概要欄に」と言い切っている。** 概要欄に無ければ、
その一行が視聴者を空振りさせる —— 制約2（誤解を与えない）に直接あたる。

ただし断定はできない。API が返すのは About の本文だけで、**リンク欄（links
section）は返らない**。前任が誘導先を概要欄にしたのは、そのリンク欄が実際の
ボタンとして出るからなので、「本文に無い」ことは「リンクが無い」ことを意味しない。
機械からはここまでしか分からない。

**人がやること：チャンネルページを開いて、リンク欄にテスターグループの
リンクが出ているか目で見る。** 無ければ足す。あるなら、この項目はもう気にしなくてよい
（次に来る側は、同じ note を毎回見ることになるので、確認済みならここに追記して
打ち切ること）。

なお動画の説明欄には参加手順もURLも入っている（`cta()` が正本）。概要欄が空でも、
説明欄を読める人には届く。届かないのは「URLがタップできないから概要欄へ」と
案内された人だけ。

### 報告は日本語で（同日・ユーザー指示）

ユーザーへの報告は日本語で書くこと。コードのコメントは英語のまま（既存に合わせる）、
日誌も日本語のまま。変わるのは**人に向けて書く報告文**だけ。
