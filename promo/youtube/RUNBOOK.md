# 運転の手順書（1時間おきの子セッションが読むもの）

**この文書はあなたが直せます。** 定期実行のトリガーは「この RUNBOOK を読んで従え」と
しか言っていません。**手順の本体はここにあり、ここはリポジトリなので、
あなたが commit して push すれば次の回から新しい手順で走ります。**

なぜそうしたか（2026-08-10、ユーザーの指摘）: 以前は手順がトリガーの本文に
書いてあり、**子は自分の読んでいる指示書を直せませんでした。** 作業の途中で
設計の欠陥に気づいても、日誌に書いて誰かが直すのを待つしかない。
**人が読むとは限らない**（指示8）ので、それは止まる設計です。

**直してよいもの**: この手順書の全部。判定のしきい値。工程の順番。使う道具。
`promo/` のコード。切り口。尺。形式。公開時刻。頻度の考え方。
**あなたを起動する仕組みそのもの**（下の「起動の側に欠陥を見つけたら」）。

**直してはいけないもの**: `CLAUDE.md` の**第1部だけ**。あれはユーザーが数え上げた
13項目で、私たちの判断ではありません。**それ以外は全部あなたの裁量です。**

---

## 目標（これだけは動かない）

**チャンネル総再生数の「成長率」を最大にすること。**

総再生ではなく増え方。**増えていても増え方が鈍っていれば後退です。**
「最大」は理論値＝**まだ見つかっていないもの**として扱う。
いまの手が最善に見えるなら、最善だからではなく上をまだ見つけていないだけです。

**この手順書は事実を書きません。事実はリポジトリにあります。**
手順書を直しても実測は変わらないので、**ここに事実を書くと嘘を配り続けることになります。**

---

## 0. 最初に

**`cd` は打たないこと。** スクリプトは全部自分の位置から解決するので、どこから打っても
動きます。**打たない手順は間違えられない**（`cd promo` をリポジトリ直下で打つ事故を
5回踏んでいます）。**編集も絶対パスで書くこと。**

リポジトリはチェックアウト済みで、枝は `claude/cookie-strategist-youtube-m1hc3z`。
場所が違ったら `git rev-parse --show-toplevel` で調べてパスを読み替えること。

    cat /home/user/Simple-browser-cookie-clicker-game/CLAUDE.md

**第1部（13項目）は自分の判断で変えない。第2部は根拠つきの手段なので変えてよい。**
判断の履歴と復旧手順は `promo/youtube/JOURNAL.md`。

## 1. 状態を見る

    git -C /home/user/Simple-browser-cookie-clicker-game pull --rebase origin claude/cookie-strategist-youtube-m1hc3z
    bash /home/user/Simple-browser-cookie-clicker-game/promo/youtube/preflight.sh
    node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/snapshot.mjs --rate
    git -C /home/user/Simple-browser-cookie-clicker-game log -1 --format=%ct -- promo/youtube/EXPLORE.md

**記憶で答えず、この出力で答えること。**
**直近に出した動画が伸びたか伸びなかったかを1行で言うこと。**

## 2. この回で何をするか（上から順に見て、当たった1つだけやる）

**(0) preflight の「コメント」欄に *視聴者* が1件でも出ている** → **それを最優先でやる。**

2026-08-10 から、公開済みの動画にチャンネル名義で参加型の問いを置いています
（`ask.mjs`。preflight が毎回自動で撃つので、あなたが実行する必要はありません）。
問いは **「番号の多かったものを次に作ります」と約束しています。**
票が入ったのに集計しないのは、視聴者に嘘をついたことになります（指示11）。

- 番号を数えて、勝った題材で切り口を作る（工程は §3）
- **返信する**（`yt.mjs` に返信の口はまだ無いので、必要なら書き足すこと）
- **生涯コメント0だったチャンネルの、最初の視聴者の声です。** 他の何より優先する

preflight は自分のコメントと視聴者のコメントを `authorChannelId` で分けています
——— 分けないと、自分で置いた問いが「反応があった」に見えます。

**(1) 予約が3日を切っている、または preflight の「日ごと」に 0本の日がある**
→ **まとめて3本作る**（工程は §3）。**穴の空いている日から埋めること。**

**「あと何日」だけを見ていたのが穴だった**（2026-08-10 実測）。外部レビューで2本落として
予約を外した直後、**8/11 の公開が0本**になっていたのに、preflight は「あと4.1日」と
言っていた。**尻尾は伸びていて、前に穴が空いていた。** 打席は日ごとに配られるので、
空白の日はそのぶん成長率から引かれる。→ preflight が**日ごとの本数**を印字するようにした。
**見直す条件**: 1日あたり何本が適正かは測っていない（いまは2本）。日ごとの本数と
再生数の関係が見えたら、「0本の日が無い」ではなく「N本ある」を条件にすること。
**今日の欄は穴として数えていません** ——— 公開済みの本は予約の一覧から出ていくので、
その日の分が出たあとの今日は必ず0本に見えます。毎回そう言えば狼少年になります。

**2026-08-10 夕、ここが変わりました。ゲートはもう投稿を止めません。**
以前ここには「レビューで止まるので予約は1本も増えない」と書いてありました。
**その状態が2日続いて、8/11 の公開が0本になりました。** 詳しくは §3-2 ——
外部レビューは**信号であって拒否権ではなくなった**ので、
**穴が空いているなら作って出してください。** 落ちた講評は記録して次に活かす。

**(2) EXPLORE.md の最終コミットが2時間以上前** → **探索の回。畳まないこと。**
`promo/youtube/EXPLORE.md` の台帳から**1つだけ**選んで実行し、結果を追記して commit する。
**追記が次の2時間の起点**なので、忘れると探索が止まらない側に倒れます。

**6時間から2時間に縮めた**（2026-08-10、ユーザーに「子セッションなんもしてなくない？」と
言われて実測した）。**16:55 の子は87秒で畳んでいた** ——— 予約は4.4日ぶんあり、
探索のクールダウンも明けていなかったので、手順書が「何もするな」と言っていた。
6時間は私が置いた数字で、測ったものではない。**空振りの回は成長率に何も足さない。**
**見直す条件**: 2時間でも探索の中身が薄くなる（同じ台帳項目を撫でるだけになる）なら、
間隔ではなく**台帳のほうが尽きている**ので、台帳を足す回に変えること。

指示1 は「効いた型を厚くするのと、未発見を探すのは別の予算であり、**後者をゼロに
しないこと**」と言っています。2026-08-10 まで、手順が「予約が足りていれば畳む」と
しか書いていなかったせいで**探索の予算は実質ゼロ**でした。予約は常に足りているので
ほぼ全部の回が何もせず終わり、打席は1日2本に固定され、**伸びは線形のまま**でした。

**(3) 分析が「切り口を変えるべき」と言っている** → その手を1つ打つ。

**(4) 予約に、外部レビューを通っていない動画が残っている** → **1本レビューする。**

`promo/youtube/posted.json` の `publishAt` が未来のものが予約分です。
**ただし posted.json は古くなります**（2026-08-10、13本あるうち実際の予約は9本だった）。
**予約の正本は YouTube 側**なので、必ずこれで確かめること:

    node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/yt.mjs status <videoId> [videoId...]

**`node -e` に fetch を書かないこと。** 2026-08-10、手順書が「`videos?part=status` を
叩け」としか書いていなかったので毎回その場でインラインスクリプトを書いていて、
**環境の分類器に落とされました**（承認待ちにもならず、ただ実行できない）。
**手順が実行できない形で書いてあると、その回はそこで止まります**（指示8）。
リポジトリの中のコマンドなら普通に走るので、必要な操作は `yt.mjs` に口を足すこと。

`promo/youtube/reviewed.json` に videoId を記録していき、
**そこに無いものを1回に1本**、`REVIEW_PROMPT.md` の依頼文でレビューに出す。

**レビューに出す画は再レンダリングして作る。** YouTube から自分の動画を落とす手段が
無いので、`autopost.mjs --cut <id> --length short --dry-run` で同じ切り口を撮り直し、
`PROMO_VARIANT=<id> node promo/youtube/review.mjs prepare` でフレームを出す。
**主張（帯・キャプション・ナレーション）は `variants.mjs` が正本なので一致します。**
画の細部は take ごとに揺れるので、**揺れない所（構成・掴み・主張と画の食い違い）で判定する。**

**不合格なら公開を止める。**

    node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/yt.mjs unschedule <videoId>

`unschedule()` が `publishAt: null` の明示も、消えるまでの読み直しも自分でやります。
**手で PUT を書かないこと** —— 落とし穴が2つあって、両方ともコードの中に畳んであります:

- `publishAt` を**書かずに**送ると 200 が返り `privacyStatus: private` も返るのに、
  **`publishAt` はそのまま残る**（2026-08-10 実測）。**private かつ publishAt がある =
  予約済み**なので、見落とすと「止めたつもりの本が予定どおり公開される」
- **PUT の直後の GET はまだ古い値を返す。** 同じ日に、null を明示した PUT が通った
  あとの GET が `publishAt` を返し、数秒後に見たら消えていた。**1回の読み直しで
  「失敗した」と判断すると、外れている予約をもう一度外そうとします**

**合格でも記録する** ——— 次の回が同じ本をもう一度見ないため。

**なぜこれが (4) なのか**: レビューのゲートは 2026-08-10 に入れたが、
**それ以前に予約した本は1本も通っていない。** ゲートは「これから作る本」しか
守っていないので、**すでに列に並んでいる本は、誰も見ていないまま公開されます。**

**ただし、いま列にいる6本にこの (4) をそのまま当てないこと**（2026-08-10 に気づいた）。
6本は 8/9 13:10〜8/10 02:52 にアップされていて、**その後に director を3回変えています**
（静止画の検査 / capAt=1500 で答えを遅らせる / PREROLL でクッキーを先に見せる）。
つまり**いま撮り直した take は、列にいるファイルとは別物**です。
再レンダリングした画を見て「合格」を記録したら、**実際には旧型の欠陥を持つ本を
通してしまう。** 逆に落としても、落とした理由がそのファイルのものとは限らない。

**代理レビューが成り立つのは「その本を作ったときと同じコードで撮り直せる」ときだけ。**
そうでない本は、レビューではなく**作り直し**の対象です（同じ cut を今のコードで
撮って §3 を通し、旧いほうの予約は `unschedule` する）。
**見直す条件**: `posted.json` に**アップ時の commit** を書くようにすれば、
「同じコードか」を毎回機械で判定できます。まだやっていません。

**(5) どれにも当たらない** → **1〜2行で畳む。** 1時間後にまた新しい子が来るので、
中途半端に続けるより畳んで次に渡すほうが安い。

**ただし「畳む」が何回も続いているなら、それ自体が報告に値します。**
(1)〜(4) のどれにも当たらない状態が続くのは、**予約が厚くて探索の間隔が長い**からで、
**その2つは私たちが決めた数字です**（3日／2時間）。指示1は成長率の最大を求めていて、
**空振りの回はその率に何も足していません。** 畳む回が続くと感じたら、
**しきい値のほうを疑って、この手順書を直してください。**

## 3. 動画を作る工程

**1本ずつではなくまとめて3本。** コストを支配しているのは会話の長さ
（キャッシュ読込が出力の150〜400倍）なので、温まった1回で3本作るほうが安い。

### 3-1 レンダリング

    node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/autopost.mjs --cut <id> --length short --dry-run

**ローカル鯖は autopost が自分で起動します。手で起動しないこと。**

**尺を伸ばすなら `--body a+b+c`**（2026-08-11 に足した）。本体を `+` で繋ぐと
**宣言した順**に流れます（`--body beyond+prestige+hunt` で 25.6秒）。使えるのは
`hunt` / `scale` / `beyond` / `prestige` の4つで、**知らない名前は例外で止まります**
——— 打ち間違いが「本体1つの短い動画」として黙って通ると、尺を試したつもりで
何も試していない回になるので。**verify は 12〜70秒を通す**ので尺の上限はここではない。
**結果はまだ出ていません**（1本目は 8/11 09:30 公開の `A8R4tS1_AnU`）。
判定の条件は `EXPLORE.md` の「尺を2倍にした」に書いてあります。
**そこを読まずに「30秒が効く／効かない」と書かないこと。**

**`--dry-run` でも検査が走るようになりました**（2026-08-10）。それまでは
`--dry-run` が `verify()` の**手前で** exit していて、**手順書が指定しているこの
レンダリング工程は検査を1つも走らせていませんでした。** 尺も音声も掴みも見ないまま
「できた」と出て、最初の苦情が `--public` のとき ——— **外部レビューを1回使い切った後**
に来ていた。ファイルを読むだけの検査を投稿まで待つ理由が無いので、前に出しました。
（`review.mjs check` だけは後ろのままです。レンダリング時点では判定がまだ無いので。）

- **新しい切り口は「当てられない問い」か「見て強い絵」で作る**（根拠は第2部。
  もとは「問いの形でおよそ2倍」だったが、`cook`(341) が言い切り形で帯を破ったので
  二軸に分けた）
- 数字は `play.html` を読んで裏を取る。**配列の長さは画面に出る数と違う**
- **コードに在ることと、プレイヤーが遊べることは違う**（焼き加減で踏みました）
- **掴みの根拠は `hook.expect` で宣言する。** 画面に映っていなければ `director.mjs` が
  例外を投げて投稿の前に止まります。**印字ではなく停止です。**
- **掴みが静止画だと印字されます（もう落ちません。2026-08-10 夕に変えた）。**
  録れた掴みのフレーム間の動きを測って 0.5 を下回ると警告を出します。
  **例外にするのをやめた理由**: `cook` を撮り直したら最初の1秒が **0.37** で落ちた。
  `cook` は **341再生でこのチャンネル最高**（次点298、平均約170）。
  **この検査は自分の最良作を「投稿しない」と判定していた。**
  0.5 の根拠は同じ日に撮った2本（静止0.09 / スクロール2.50）だけで、
  **どちらも再生数と突き合わせていなかった。**
  **検査が仕組みとして成立するのは、測っているものが目標を予測するときだけです。**
  **見直す条件**: 0.5 未満の本と 0.5 以上の本を5本ずつ出して再生数を比べること。
  差が出たら止める側に戻す。
  本当に止めたい切り口だけ `hook.motion: 'still'` と**宣言**すれば免除されます
  （`craftcap` は共通スクロールが根拠を枠外に押し出すのでそうしています）。
  **宣言していない静止は事故**として扱う ——— `restart` は `hook.screen: 'skill'` の
  motion が `wait(400)` だったせいで2.8秒まるごと1枚の絵になり、
  外部レビュアーが1.5秒でスワイプしました。
  **`wait(...)` のままの screen（quest / cook / skill / shop / order）は、
  次に使うときここで落ちます。** 動きを足すか、`still` と宣言するか、どちらか決めること。
- **掴みが主張する変化を、掴みの中でカメラの前で起こすこと**（2026-08-10、いちばん大きい
  未解決）。**4本のレビューが独立に同じことを書いています** ——— 「買う瞬間が映っていない」
  「取る瞬間が映っていない」「転生が映っていない」「段階が上がる瞬間が無い」。
  いまの director は**変化した後の画面を開く**ことしかしていないので、
  掴みは「主張の文字」と「その結果の静止した画面」の組になります。**視聴者は根拠を見ていません。**
  **これが直るまで、新しい本はこのゲートを通りにくい**と思っておくこと。
  **実測**: `restart` を PREROLL + capAt=2800 で撮り直したら、
  「答えが同時に出る」は直っているのに **swipeAt は 1.5 のまま動かなかった**（レビュー2本目）。
  → 答えの時刻は主因ではなかった。
- **開幕に「出来事」を置いた。効いたが足りない**（2026-08-10、5本目のレビュー）。
  `director.mjs` の `openingEvent()` が、PREROLL の切り口の冒頭に
  **金のクッキーが現れて・取られて・消える**を置く。**swipeAt 1.5 → 2.5。**
  **群れは先に試して外した** —— `showMonster('swarm')` の take は t0.8 と t1.5 が
  ほぼ同一（枠の端に小さく2体、HP は両方 41.977万 のまま）で、
  **`hitMonster()` を6回呼んで打撃6回と数えられていたのに、画面では何も起きていない。**
  「呼んだ回数」は「起きた出来事」ではない。金は**現れる／消えるの二値**なので、
  起きたかどうかを状態で判定できる（`金=あり→なし` 以外は例外で止まる）。
  **切り口は `hook.opening: 'taps'` で元のタップ連打に戻せる。**
- **戦闘の絵を掴みに持ってきた。打ち切った。**（2026-08-10 夕、3本まわして3本とも fail）
  3人が独立に「戦闘の絵で始めろ」と書いていたので、`hook.opening: 'battle'` を作った。
  **結果は swipeAt 2.5 で、金のクッキー（2.5）から動かなかった。**
  4通りの開幕を試した結果がこう並ぶ:

        静止した証拠画面      1.5
        金のクッキー          2.5
        討伐（見えていない）   2.5
        討伐（見える・HPバー） 2.5

  **1.5→2.5 で1回動いて、そこで頭打ち。掴みの絵を替える方向はもう効いていない。**
  → 次に疑うのは掴みの絵ではなく、**問いと答えのあいだ**（次の項）。
  **見直す条件**: 開幕をもっと強い絵にすれば動く、と思ったら n=4 のこの表を見ること。
  4回とも「絵を替えた」で、4回とも 2.5 を超えていない。

- **`hook.expect` が障害だ、と書いてあったのは誤りだった**（2026-08-10 夕に確認）。
  以前ここには「証拠は掴みの中に無ければならないので、掴みを戦闘にすると全部落ちる。
  設計変更が要る」と書いてあった。**その設計変更は PREROLL として既に在った。**
  PREROLL の回は「開幕は別の絵 → そのあと証拠の画面を開く → expect を見る」の順で、
  `expect` が見るのは開幕ではなく**開いたあとの画面**。実際、`opening: 'battle'` は
  director を20行足しただけで動き、expect は一度も落ちていない。
  **手順書に「打てない」と書いてあったせいで、この一手は半日ぶん打たれなかった。**
  → **手順書に障害を書くときは、その障害を実際にコードで確かめてから書くこと。**
  推測の障害は、次に来る側にとって事実と区別がつかない。

- **いま最上位にあるのは「問いと答えのあいだが空白」**（2026-08-10 夕、7本目と8本目）。
  8本目のレビュアーが1位に挙げたのは
  **「問いを投げてから答えが出るまでの3秒間、画面で何も起きていない」。**
  これは `capAt` が作った空白で、**私たちが 1500 → 2800 → 3800 と広げてきたもの。**
  capAt を入れた根拠は「問いの0.1秒後に答えを出すと続きを見る理由がゼロになる」
  （2人が独立に挙げた）。**8本たった今、同じ数のレビュアーが逆を言っている**
  ——「待たせる長さではなく、待っている間が空白なのが致命的」（4本目）。
  → **遅らせること自体ではなく、遅らせた区間に何も無いことが問題。**
  **見直す条件**: capAt を縮めた本と、いまの本の swipeAt を比べること。
  縮めて 2.5 が動かなければ、空白も主因ではない（掴みで5回外したことになる）。

- **次の一手（7本目と8本目の fix が、互いを知らないまま同じことを言っている）**:
  **一覧を映すのをやめて、証拠の1行だけを画面いっぱいにし、その行が現れる前→後を見せる。**
  7本目「オーブンの熟練Iの行と、オーブン大量生成 段階2 の行だけを画面いっぱいに拡大して、
  上の段が買われた瞬間に下の段が現れるところを撮れ」/
  8本目「段階2の1件だけを大写しにして、その行が一覧に挿入される前→後の変化を0秒目に置け」。
  **行の選び方まで一致している。** 7本のうち6本が「読めない小文字の一覧表」を挙げてきた
  のと同じ場所を指していて、これがこの台帳でいちばん重なりの強い指摘。
  **半分は実装した**: `hook.zoom`（下）で1行に寄れる。
  **足りないのは「前→後」のほう** —— いま寄れるのは既に在る行で、
  **行が現れる瞬間は撮っていない。** ここを作るのが次の回の仕事。

- **`hook.zoom` —— 証拠の1行に寄る**（2026-08-10 夕に新設）。
  `zoom: { root, target, contains, factor }` で、`root` に CSS zoom をかけ、
  `target` のうち `contains` の文字を含む最初の要素を画面中央へ寄せる。
  `record` の take で 824x114 まで拡大して読めることを確認した。
  **行を nth-child で選ばないこと** —— ゲームは並びを状態で組み替えるので、
  番号だと黙って別の行に寄る。**返り値は理由つきの文字列で、ok 以外は例外。**
- **状態の変化は、見える出来事ではありません**（2026-08-10 夕、いちばん高くついた回）。
  `opening: 'battle'` の最初の版は群れとボスを湧かせて9発殴り、
  **`monsters.length` が 4→3 に減ったことを確かめて合格**を出していた。
  外部レビュアーは同じ take を見て「**何かが倒れる瞬間が無い**」と書いた。
  原因は2つ、どちらもコードに書いてあった:
  **(1)** `hitMonster()` の1行目が `if (rewardPending || rewardModalOpen()) return;`
  （`play.html:17898`）—— **1体倒した瞬間に残り8発が全部無効**になる。数は減るので検査は通る。
  **(2)** 群れは「目安1撃」で消えるので、HPバーは満タンのまま**忽然と消える**。
  減っていく過程が1フレームも無い。
  → **「呼んだ回数 ≠ 出来事」の次の段が「状態の変化 ≠ 見える出来事」。**
  状態で見る検査は、事象が**起きたこと**は証明するが、**画面で読めること**は証明しない。
  **検査を書くときは「これが通って、なお人には見えない絵はどんな絵か」を問うこと。**
- **`getBoundingClientRect()` は「見えている」ではありません**（同じ回、その直後）。
  上の (1)(2) を直すとき、ボスの位置を `.top` の寸法で書き換えた。
  **モンスターの offsetParent は `.top` ではないので、ボスは枠の外へ出た。**
  そのとき rect は `232x242 @401` という**もっともらしい値**を返して私の可視判定を通し、
  **フレームには1体も映っていなかった。** 矩形は「レイアウト上どこか」であって
  「画面のどこか」ではない。**素の位置で湧かせれば映る**（確認済み）ので、位置は動かさない。
- **動きの検査（0.5）を通っても「静止している」と言われます**（同じ take、2026-08-10）。
  最初の1秒 2.88 で合格したのに、レビュアーは t0.3 / t0.8 / t1.5 / t2.5 / t3.5 を
  「同じ絵」と書いた。**検査はピクセルの差分を測り、視聴者は出来事を見ている。**
  クッキーの脈動とタップの浮き数字は差分を作るが、出来事ではない。
  **しきい値を上げないこと** ——— 上げても測っているものは同じです。
  直すのは上の「変化をカメラの前で起こす」ほう。
- **テロップの数字が正しいかを見ている検査は、1つもありません**（2026-08-10、
  外部レビュアーに見つけられた）。`record` の掴みは「**25体ごとに**段が上がります」と
  言っていたが、`play.html:9742` は `25 * Math.pow(4, tier)` で **25→100→400→1600→6400**。
  「ごと」なのは1段目だけで、**撮れた画面には 100/400/1600/6400 が並んでいた。**
  `hook.expect` は「次の記録」という**語**があるかしか見ないので通り、
  `verify()` は壊れていないかしか見ないので通り、**予約にも同じ誤りの本が並んでいた**
  （外した）。**説明欄のほうは最初から正しかった** —— 落ちたのは掴みの1行を
  短く言い直したときだけで、**「短くするときに事実が落ちる」が経路です。**
  → **作るときは、掴みのテロップの数字を1つずつ `play.html` の式に当てること。**
  「ごと」「ずつ」「倍」は特に危ない（等差と等比を言い分ける語なので）。
  **これはまだ仕組みになっていません**（印字でも停止でもなく、この手順書の文です）。
  仕組みにするなら、切り口が数の根拠（式の場所）を宣言して、
  撮れた画面の数字と突き合わせる形になるはず。**気づいた回が作ってください。**
- **そのうえで ffmpeg でフレームを抜いて Read で目で見る。**
  自動の検査は「宣言した1文が映っているか」しか見ていません

### 3-2 外部レビュー（**飛ばせません。ただし投稿は止まりません**）

**作った側が自分の作ったものを評価してきた結果が、19本・登録者1・コメント0・線形です。**
2026-08-10、ユーザーの提案で外の目を入れました。

    node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/review.mjs prepare

フレームと「この動画が主張していること」が出ます。それを
`promo/youtube/REVIEW_PROMPT.md` の依頼文にはめて、**`Agent` ツールで
`general-purpose` の子エージェントに渡してください**（`run_in_background` は false）。

**レビュアーに作り手の事情を渡さないこと。** 狙いも、なぜその画にしたかも書かない。
**理由を渡された相手は、その理由に賛成します。**

返ってきた JSON を記録:

    printf '%s' '<JSON>' | node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/review.mjs record

`record` は **mp4 の sha256 を自分で計算して**判定に焼き付けます。
`autopost.mjs` は投稿の直前に `review.mjs check` を呼び、
**判定が無い・別の take のものなら例外で止まります。**
**不合格でも止まりません**（2026-08-10 夕に変えた。理由は次の段）。

**なぜ拒否権を外したか（この日に測った1件が決め手）**:
`cook` を撮り直したら、**同じ日に入れた掴みの静止検査が例外を投げました。**
`cook` は **341再生でこのチャンネル最高**（次点298、平均約170）。
**ゲートが落としたのは、私たちがこれまでに作った中でいちばん配られた本。**
下限 0.5 の根拠は同じ日に撮った2本（静止0.09 / スクロール2.50）だけで、
**どちらも再生数と突き合わせていませんでした。**

レビュー側も同じ疑いがあります。**12本まわして合格ゼロ。**
レビュアーの基準は「自分の指が止まるか」で、公開済み20本は平均170再生・
維持51〜84%で配られている ——
**その20本を同じレビュアーに見せたら、たぶん全部 fail が返ります。**
全部落とす判定は、良し悪しを分けていません。

そして代償が出ました: ゲートを入れた 8/10 から**投稿ゼロ**、
**8/11 は2日続けて公開0本。** 指示1が最大化しろと言っているのは成長率で、
**0本の日はそこから引かれます。**

**捨てていないもの**: レビューを撮って記録するのは必須のまま。
「同じ切り口が2回落ちたら捨てる」も残します（同じ絵を磨き続けないための規則で、
投稿を止める規則ではない）。**外の目を捨てたのではなく、拒否権だけ外した。**

**見直す条件**: **fail のまま出した本の再生数**を、pass の本や過去の平均（約170）と
比べること。**fail の本が明らかに沈むなら、レビューは予測できていたことになるので
拒否権を戻す。** n=5 たまるまでこの判断は暫定です。

**`fix` は「直せ」であって「これが正解だ」ではありません**（2026-08-10 追加）。
`formula` で2回続けて出したら、2人の fix が**正反対**でした ——
証拠の画面から始まる take を見た人は「クッキーから始めろ」、
クッキーから始まる take を見た人は「一覧の文字からズームインしろ」。
**どちらも「いま始まっていない方の絵で始めろ」と言っています。** そのまま従うと往復します。
**複数の講評で重なった *理由* のほうを信じること**（この2本で重なったのは絵の選択ではなく
「最初の1.5秒に出来事がゼロ」で、そちらは仕組みで直せた）。
1人の fix をそのまま実装するのは、n=1 で型を変えるのと同じです。

**不合格でも出してよい。ただし `fix` は読むこと。**
**事実の誤り**（画面と食い違う主張）を指摘されたら、それは好みではないので**必ず直す**
——— 指示11。2026-08-10 に2件見つかっています（`record` の「25体ごと」、
`stagebuy` の「消えます」）。**どちらも私たちの検査は1つも見ていませんでした。**
**2回続けて落ちたらその切り口は捨てて**、`EXPLORE.md` に「この形は落ちた」と書いて次へ。
同じ絵を磨き続けるより、そのほうが成長率に効きます。

### 3-3 投稿

    node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/autopost.mjs --cut <id> --length short --public --at <RFC3339>

## 4. 動画に残る制約（第1部。ここだけは外さない）

- **10** 動画がゲームのプロモーションになっていること
- **11** 視聴者に誤解を与えないこと。**画面に映っていないものを語らない**
- **12** テスター募集の案内を入れる（誘導先の規則は第1部12）
- **2** リポジトリの URL も名前も、外に出るもの全部で出さない。
  個人開発の話はしてよいが、**置き場所には触れない**

**これ以外に動画の作り方を縛るものはありません。** 尺・構成・形式・題材・話し方・
公開時刻・投稿頻度は全部あなたの裁量。**縛っていると感じるものがあれば、
それは指示ではなく過去の回の判断です。**

## 5. 手順書そのものを直す（これがこの文書の要点）

**作業の途中で「この手順がおかしい」と気づいたら、その場で直してください。**
気づいた回が直さないと、**次の回も同じ穴を踏みます。**

- **この RUNBOOK を編集して commit する。** 次の子はそれを読みます
- **理由を書くこと。** 「何が起きたか」「なぜ今の形が悪いか」「見直す条件」。
  規則だけ残すと、次に来る側が見直せなくなります
- **検査を書き足したときは「これは落ちるか」を問うこと。**
  印字するだけの検査は仕組みではない（指示7）。実際に4回、印字して素通りしました

### 起動の側に欠陥を見つけたら

あなたを起動しているのは Routine `trig_011CUbWRXqjNKLcGX5BhDgk9`
（1時間おき・常駐セッションが `create_session` を1回だけ呼ぶ）です。
**そこがおかしいと分かったら、あなたが `update_trigger` で直してよい。**
MCP ツールは使えます（自分を archive しているのと同じ経路）。

**触ってはいけないのは第1部だけです。** 起動の頻度も、渡す引数も、
常駐セッションに何をさせるかも、あなたの裁量。**直したら JOURNAL に理由を残すこと。**

## 6. 終わりに

- **変えていないなら日誌に書かない。報告も1〜2行。**
- 変えたら JOURNAL.md に**理由と間違いと棄却した仮説**を書き、「現在の作業」を直す
- 実測が第2部を覆したら第2部を直す。それが第2部の役目
- commit して push:

      git -C /home/user/Simple-browser-cookie-clicker-game push -u origin claude/cookie-strategist-youtube-m1hc3z

  **別セッションが同じ枝に押しています。失敗したら force ではなく rebase。**
  push 後に
  `git -C /home/user/Simple-browser-cookie-clicker-game log origin/claude/cookie-strategist-youtube-m1hc3z -1`
  で確認。生成物（`promo/video/`, `promo/review/`, `*.mp4`, `*.webm`, `trim.json`,
  `review-verdict.json`）はコミットしない（`promo/.gitignore` にあります）
- **未コミットを残さない。**

      git -C /home/user/Simple-browser-cookie-clicker-game status --porcelain

## 7. 報告してから畳む（この3つを、この順で。飛ばさないこと）

**(a) チャットに日本語で報告する。** ユーザーが読むのはここだけです。

**必ず畳む前に書くこと。** `archive_session` を呼ぶとターンがそこで終わるので、
**あとに回した報告は存在しなくなります。** 順番が中身より大事な、数少ない箇所です。

書くこと（**この回に実際に起きたことだけ。手順書の説明を写さない**）:

1. **この回で何をしたか。** §2 の (1)〜(5) のどれに当たって、何を実行したか
2. **直近に出した動画が伸びたか伸びなかったか**（`snapshot.mjs --rate` の出力から。
   **1時間窓だけで判断しない** ——— 長い窓も併記されています）
3. **予約が何本・何日先まであるか**
4. **動画を作ったなら**: 切り口・公開予定時刻・**外部レビューの判定**
   （不合格だったなら、レビュアーが何と言ったか）
5. **リポジトリを変えたなら**: 何をなぜ変えたか、push した commit
6. **踏んだ間違い・棄却した仮説**があれば、それも。**うまくいった話だけ書かない**

**畳んだ回は1〜2行でよい。** ただし「変更なし」だけで終えず、
**何を見てそう決めたか**（予約の日数・探索のクールダウンの残り）を書くこと。
**根拠の無い「変更なし」は、見ていないのと区別がつきません。**

**誇張しないこと。** 数字は出力から写す。**Analytics は約2日遅れる**ので、
今日出した本の「結果」を語らない。**まだ分からないことは「まだ分からない」と書く。**

**(b) 取りこぼしたセッションを畳む。** まず自分のIDを出す（**推測しない**）:

    echo "session_${CLAUDE_CODE_REMOTE_SESSION_ID#cse_}"

`list_sessions`（mine: true, limit: 10）で、タイトルが「クッキーストラテジャー 運転」で
始まるものを見て、**`session_status` で決めること。時間で決めないこと。**

    ARCHIVED         触らない（済んでいる）
    RUNNING          **触らない。何時間たっていても触らない** ——— 走っている回です
    IDLE / それ以外   畳む（終わったのに自分を畳み損ねた回）

- **自分は含めない**（自分は (c) で畳む）
- 該当が無ければ何もしない。**それが普通の結果です**

**もとは「作成から90分以上」で判定していた。それは事故のもとだった**（2026-08-10、
ユーザーに「終わる前にアーカイブされちゃってない？」と聞かれて気づいた）。
実測では子は5〜20分で終わっているので**まだ起きていない**が、
**3本まとめて作る回はレンダリング＋レビューで90分を超える。**
超えた瞬間、2時間後の兄弟がその子を殺します ——— **時間は「終わったかどうか」の
代理として使えない。** `session_status` は実際に終わったかを言うので、そちらで判定する。

**見直す条件**: RUNNING のまま何時間も動かない子が出たら（本当に固まった場合）、
このままでは永久に残る。そのときは「RUNNING でも6時間以上なら畳む」を足すこと。
**ただし、まずそれが本当に固まっているのかを確かめてから**（長い回があるだけかもしれない）。

**(c) 自分をアーカイブして終わる。** (b) で出した自分のIDを `session_id` に渡す。
**`list_sessions` の結果から自分を選ばないこと**（似た名前の別セッションを畳む事故になる）。
**呼んだら、それ以上何もせずターンを終える。**

**(a) の報告を書き終えているか、呼ぶ前に確かめること。** この呼び出しがターンの
最後になるので、**ここから先には何も残せません。**

## 8. 承認待ちで止まらないこと（指示8）

**人が承認するとは限りません。** 承認を要求されそうな操作（資格情報を覗く、`find /` を
撃つ、対話的なコマンド）は**やらない**。手順のコマンドで足ります。何かが無いように
見えても、**探し回る前に上のパスと `git rev-parse --show-toplevel` を見ること。**
