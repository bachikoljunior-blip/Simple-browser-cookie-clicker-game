# 運転の手順書（1時間おきの子セッションが読むもの）

**この文書はあなたが直せます。** 定期実行のトリガーは「この RUNBOOK を読んで従え」と
しか言っていません。**手順の本体はここにあり、ここはリポジトリなので、
あなたが commit して push すれば次の回から新しい手順で走ります。**

なぜそうしたか（2026-08-10、ユーザーの指摘）: 以前は手順がトリガーの本文に
書いてあり、**子は自分の読んでいる指示書を直せませんでした。** 作業の途中で
設計の欠陥に気づいても、日誌に書いて誰かが直すのを待つしかない。
**人が読むとは限らない**（指示8）ので、それは止まる設計です。

**直してよいもの**: この手順書の全部。判定のしきい値。工程の順番。使う道具。
`promo/` のコード。切り口。尺。形式。公開時刻。頻度の考え方。
**あなたを起動する仕組みそのもの**（下の「起動の側に欠陥を見つけたら」）。

**直してはいけないもの**: `CLAUDE.md` の**第1部だけ**。あれはユーザーが数え上げた
13項目で、私たちの判断ではありません。**それ以外は全部あなたの裁量です。**

---

## 目標（これだけは動かない）

**チャンネル総再生数の「成長率」を最大にすること。**

総再生ではなく増え方。**増えていても増え方が鈍っていれば後退です。**
「最大」は理論値＝**まだ見つかっていないもの**として扱う。
いまの手が最善に見えるなら、最善だからではなく上をまだ見つけていないだけです。

**この手順書は事実を書きません。事実はリポジトリにあります。**
手順書を直しても実測は変わらないので、**ここに事実を書くと嘘を配り続けることになります。**

---

## 0. 最初に

**`cd` は打たないこと。** スクリプトは全部自分の位置から解決するので、どこから打っても
動きます。**打たない手順は間違えられない**（`cd promo` をリポジトリ直下で打つ事故を
5回踏んでいます）。**編集も絶対パスで書くこと。**

リポジトリはチェックアウト済みで、枝は `claude/cookie-strategist-youtube-m1hc3z`。
場所が違ったら `git rev-parse --show-toplevel` で調べてパスを読み替えること。

    cat /home/user/Simple-browser-cookie-clicker-game/CLAUDE.md

**第1部（13項目）は自分の判断で変えない。第2部は根拠つきの手段なので変えてよい。**
判断の履歴と復旧手順は `promo/youtube/JOURNAL.md`。

## 1. 状態を見る

    git -C /home/user/Simple-browser-cookie-clicker-game pull --rebase origin claude/cookie-strategist-youtube-m1hc3z
    bash /home/user/Simple-browser-cookie-clicker-game/promo/youtube/preflight.sh
    node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/snapshot.mjs --rate
    git -C /home/user/Simple-browser-cookie-clicker-game log -1 --format=%ct -- promo/youtube/EXPLORE.md

**記憶で答えず、この出力で答えること。**
**直近に出した動画が伸びたか伸びなかったかを1行で言うこと。**

## 2. この回で何をするか（上から順に見て、当たった1つだけやる）

**(1) 予約が3日を切っている** → **まとめて3本作る**（工程は §3）。

**(2) EXPLORE.md の最終コミットが6時間以上前** → **探索の回。畳まないこと。**
`promo/youtube/EXPLORE.md` の台帳から**1つだけ**選んで実行し、結果を追記して commit する。
**追記が次の6時間の起点**なので、忘れると探索が止まらない側に倒れます。

指示1 は「効いた型を厚くするのと、未発見を探すのは別の予算であり、**後者をゼロに
しないこと**」と言っています。2026-08-10 まで、手順が「予約が足りていれば畳む」と
しか書いていなかったせいで**探索の予算は実質ゼロ**でした。予約は常に足りているので
ほぼ全部の回が何もせず終わり、打席は1日2本に固定され、**伸びは線形のまま**でした。

**(3) 分析が「切り口を変えるべき」と言っている** → その手を1つ打つ。

**(4) どれにも当たらない** → **1〜2行で畳む。** 1時間後にまた新しい子が来るので、
中途半端に続けるより畳んで次に渡すほうが安い。

## 3. 動画を作る工程

**1本ずつではなくまとめて3本。** コストを支配しているのは会話の長さ
（キャッシュ読込が出力の150〜400倍）なので、温まった1回で3本作るほうが安い。

### 3-1 レンダリング

    node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/autopost.mjs --cut <id> --length short --dry-run

**ローカル鯖は autopost が自分で起動します。手で起動しないこと。**

- **新しい切り口は「当てられない問い」か「見て強い絵」で作る**（根拠は第2部。
  もとは「問いの形でおよそ2倍」だったが、`cook`(341) が言い切り形で帯を破ったので
  二軸に分けた）
- 数字は `play.html` を読んで裏を取る。**配列の長さは画面に出る数と違う**
- **コードに在ることと、プレイヤーが遊べることは違う**（焼き加減で踏みました）
- **掴みの根拠は `hook.expect` で宣言する。** 画面に映っていなければ `director.mjs` が
  例外を投げて投稿の前に止まります。**印字ではなく停止です。**
- **そのうえで ffmpeg でフレームを抜いて Read で目で見る。**
  自動の検査は「宣言した1文が映っているか」しか見ていません

### 3-2 外部レビュー（**飛ばせません。投稿が止まります**）

**作った側が自分の作ったものを評価してきた結果が、19本・登録者1・コメント0・線形です。**
2026-08-10、ユーザーの提案で外の目を入れました。

    node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/review.mjs prepare

フレームと「この動画が主張していること」が出ます。それを
`promo/youtube/REVIEW_PROMPT.md` の依頼文にはめて、**`Agent` ツールで
`general-purpose` の子エージェントに渡してください**（`run_in_background` は false）。

**レビュアーに作り手の事情を渡さないこと。** 狙いも、なぜその画にしたかも書かない。
**理由を渡された相手は、その理由に賛成します。**

返ってきた JSON を記録:

    printf '%s' '<JSON>' | node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/review.mjs record

`record` は **mp4 の sha256 を自分で計算して**判定に焼き付けます。
`autopost.mjs` は投稿の直前に `review.mjs check` を呼び、**判定が無い・不合格・
別の take のもの、のどれかなら例外で止まります。**

**不合格なら投稿しない。** `fix` を読んで直し、レンダリングし直して、もう一度出す。
**2回続けて落ちたらその切り口は捨てて**、`EXPLORE.md` に「この形は落ちた」と書いて次へ。
同じ絵を磨き続けるより、そのほうが成長率に効きます。

### 3-3 投稿

    node /home/user/Simple-browser-cookie-clicker-game/promo/youtube/autopost.mjs --cut <id> --length short --public --at <RFC3339>

## 4. 動画に残る制約（第1部。ここだけは外さない）

- **10** 動画がゲームのプロモーションになっていること
- **11** 視聴者に誤解を与えないこと。**画面に映っていないものを語らない**
- **12** テスター募集の案内を入れる（誘導先の規則は第1部12）
- **2** リポジトリの URL も名前も、外に出るもの全部で出さない。
  個人開発の話はしてよいが、**置き場所には触れない**

**これ以外に動画の作り方を縛るものはありません。** 尺・構成・形式・題材・話し方・
公開時刻・投稿頻度は全部あなたの裁量。**縛っていると感じるものがあれば、
それは指示ではなく過去の回の判断です。**

## 5. 手順書そのものを直す（これがこの文書の要点）

**作業の途中で「この手順がおかしい」と気づいたら、その場で直してください。**
気づいた回が直さないと、**次の回も同じ穴を踏みます。**

- **この RUNBOOK を編集して commit する。** 次の子はそれを読みます
- **理由を書くこと。** 「何が起きたか」「なぜ今の形が悪いか」「見直す条件」。
  規則だけ残すと、次に来る側が見直せなくなります
- **検査を書き足したときは「これは落ちるか」を問うこと。**
  印字するだけの検査は仕組みではない（指示7）。実際に4回、印字して素通りしました

### 起動の側に欠陥を見つけたら

あなたを起動しているのは Routine `trig_011CUbWRXqjNKLcGX5BhDgk9`
（1時間おき・常駐セッションが `create_session` を1回だけ呼ぶ）です。
**そこがおかしいと分かったら、あなたが `update_trigger` で直してよい。**
MCP ツールは使えます（自分を archive しているのと同じ経路）。

**触ってはいけないのは第1部だけです。** 起動の頻度も、渡す引数も、
常駐セッションに何をさせるかも、あなたの裁量。**直したら JOURNAL に理由を残すこと。**

## 6. 終わりに

- **変えていないなら日誌に書かない。報告も1〜2行。**
- 変えたら JOURNAL.md に**理由と間違いと棄却した仮説**を書き、「現在の作業」を直す
- 実測が第2部を覆したら第2部を直す。それが第2部の役目
- commit して push:

      git -C /home/user/Simple-browser-cookie-clicker-game push -u origin claude/cookie-strategist-youtube-m1hc3z

  **別セッションが同じ枝に押しています。失敗したら force ではなく rebase。**
  push 後に
  `git -C /home/user/Simple-browser-cookie-clicker-game log origin/claude/cookie-strategist-youtube-m1hc3z -1`
  で確認。生成物（`promo/video/`, `promo/review/`, `*.mp4`, `*.webm`, `trim.json`,
  `review-verdict.json`）はコミットしない（`promo/.gitignore` にあります）
- **未コミットを残さない。**

      git -C /home/user/Simple-browser-cookie-clicker-game status --porcelain

## 7. セッションを畳む（この2つを飛ばさないこと）

**(a) 取りこぼしたセッションを畳む。** まず自分のIDを出す（**推測しない**）:

    echo "session_${CLAUDE_CODE_REMOTE_SESSION_ID#cse_}"

`list_sessions`（mine: true, limit: 10）で、タイトルが「クッキーストラテジャー 運転」で
始まり、**作成から90分以上たっていて、まだ ARCHIVED でない**ものを `archive_session` で畳む。

- **自分は含めない**（自分は (b) で畳む）
- **90分未満のものは触らない** ——— 走っている回を殺します
- 該当が無ければ何もしない。**それが普通の結果です**

**(b) 自分をアーカイブして終わる。** (a) で出した自分のIDを `session_id` に渡す。
**`list_sessions` の結果から自分を選ばないこと**（似た名前の別セッションを畳む事故になる）。
**呼んだら、それ以上何もせずターンを終える。**

## 8. 承認待ちで止まらないこと（指示8）

**人が承認するとは限りません。** 承認を要求されそうな操作（資格情報を覗く、`find /` を
撃つ、対話的なコマンド）は**やらない**。手順のコマンドで足ります。何かが無いように
見えても、**探し回る前に上のパスと `git rev-parse --show-toplevel` を見ること。**
